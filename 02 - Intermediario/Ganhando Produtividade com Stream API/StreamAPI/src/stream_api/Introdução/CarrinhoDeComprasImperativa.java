package stream_api.Introdução
        ;
/*import java.util.ArrayList;


/*Na forma imperativa, para realizar uma soma simples, por exemplo, o desenvolvedor tem que se preocupar não apenas com o que deve ser feito em cada elemento, isto é, com as regras associadas ao processamento dos elementos da lista, mas também com a maneira de realizar essa iteração.*/

// Exemplo de programação imperativa
/*public class CarrinhoDeComprasImperativa {
    //atributos
    private List<Item> itemList;

    //construtor

    public CarrinhoDeComprasImperativa() {
        this.itemList = new ArrayList<>();
    }

    //Método para calcular valor total dos itens
    public double calcularValorTotal(){
        double valorTotal = 0d;
        if(!itemList.isEmpty()){
            for(Item item : itemList){
                double valorItem = item.getPreco() * valorTotal += valorTotal += valotItem;
            }
            return valorTotal;
        }else {
            throw new RuntimeException("A lista esta vazia!");
        }
    }
}*/
