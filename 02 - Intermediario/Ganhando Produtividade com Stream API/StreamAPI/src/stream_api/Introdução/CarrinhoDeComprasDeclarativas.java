package stream_api.Introdução;

import java.util.ArrayList;
import java.util.List;
// Exemplo de programação declarativa
/*public class CarrinhoDeComprasDeclarativas {
    //atributos
    private List<Item> itemList;

    //construtor
    public CarrinhoDeComprasDeclarativas() {
        this.itemList = new ArrayList<>();
    }

    //Método para calcular valor total dos itens
    public double calcularValorTotal(){
        if(itemList.isEmpty()){
            throw new RuntimeException("A lista esta vazia!");
        }
        return itemList.stream().mapToDouble(item -> item.getPreco().sum();
    }
}*/
